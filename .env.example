# Supabase is optional. Without these the app runs entirely on localStorage.
# Set both to sync the persisted state (bookings, ledgers, messages) to Supabase.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
